1) bundle install

2) rake db:create, db:migrate, db:seed

3) ( The seeds file will print out the 2 urls that can be used to test the restaurants endpoint and the menu_items endpoint. )

4) rails s

5) Hire Joe
